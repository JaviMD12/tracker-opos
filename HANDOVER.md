# Handover — Tracker Analítico de Oposiciones

**Fecha del corte:** 2026-08-22 (sincronizado sobre el corte del 2026-08-21 + el despliegue real al VPS del módulo de Flashcards con repetición espaciada (Plan Pro) y la categoría nueva "Provincia" (cartografía/historia de Huelva), compartida por Simulacros y Flashcards — commit `d4924cf`, **ya desplegado y verificado en producción**, incluidos dos fallos transitorios reales de Gemini (429 cuota, 503 servicio caído) durante la reconstrucción del índice RAG del VPS, resueltos con un script de reintento. Ver [10-flashcards-y-provincia.md](docs/handover/10-flashcards-y-provincia.md) y el punto 37 de [07](docs/handover/07-deuda-tecnica-y-pendientes.md))
**Propósito:** punto de entrada único para retomar el proyecto en una sesión nueva sin contexto previo. Sustituye a los antiguos `HANDOVER.md`, `handover_v2.md` y `handover_v3.md` (su contenido está absorbido y actualizado en la estructura de abajo) — si ves alguno de esos tres archivos sueltos en la raíz del repo, son restos de sesiones anteriores, este documento es el que manda.

**Cómo usar este documento:** este archivo es solo el índice. El detalle real vive en `docs/handover/`, dividido por subsistema para que cada sesión pueda cargar solo lo que necesita en vez de un único archivo gigante. Cada archivo enlaza de vuelta aquí arriba y a los demás archivos relacionados — sigue los links en vez de buscar todo en un solo sitio.

---

## Lo más importante antes de tocar nada

1. **El proyecto está desplegado de verdad en producción**: VPS de Hostinger (`187.55.229.111`, hostname `srv1835603`), dominio público **`https://opotracker.tech`** (live), Postgres real (no SQLite — local sigue en SQLite, son bases de datos distintas), servicio systemd `tracker-opos.service`. Esto ya **no** es un plan a futuro ni algo "nunca desplegado" — está corriendo y se ha verificado repetidamente.
2. **Acceso SSH al VPS no es persistente entre sesiones de Claude.** Se genera un par de claves dedicado en el scratchpad de cada sesión, que desaparece al empezar una nueva — hay que regenerar el par y pedirle al usuario que autorice la clave pública otra vez desde su propia terminal ya abierta en el VPS. No hay contraseña de root guardada en ningún sitio.
3. **Despliegue**: `git push` (repo `https://github.com/JaviMD12/tracker-opos.git`, rama `main`) → `git pull` por SSH en el VPS → `systemctl restart tracker-opos.service` **solo si cambió código Python** (el frontend estático se sirve en caliente, sin reiniciar).
4. **Stripe está en modo test** (`sk_test_...`) en local y en el VPS — verificar esto explícitamente antes de cualquier prueba de pago, nunca asumirlo.
5. El motor de IA es **Gemini de punta a punta** (`gemini-2.5-flash` + `models/gemini-embedding-001`) — el proyecto usó OpenAI en su día, pero ya no queda ni una referencia funcional a `openai`/`langchain-openai`/`tiktoken` en el código.
6. 🔴 **El checkout real de Stripe está PAUSADO en producción ahora mismo** (`ENABLE_STRIPE=false` activo en el `.env` del VPS desde el 2026-08-10, decisión deliberada del usuario, no un bug). Los botones "Desbloquear Premium" abren un modal de lista de espera en vez de cobrar. Comprobar esto ANTES de investigar cualquier cosa rara relacionada con pagos — ver [02-autenticacion-y-pagos.md](docs/handover/02-autenticacion-y-pagos.md).

## Resumen en una frase

SaaS FastAPI + SQLite (local) / Postgres (producción real en VPS) + frontend Vanilla JS para opositores a bombero/emergencias (foco Huelva/Andalucía): auth multitenant con `is_pro` activado por un **webhook real de Stripe** (verificado con un pago de prueba completo de extremo a extremo), dashboard gratuito de rendimiento físico/teórico (con contenido real de entrenamiento/técnicas de estudio, ya no solo un teaser, más heatmap y **3 componentes de upsell hacia Premium**), y una Zona Premium en 5 pestañas — Tablón de Plazas, Tutor Inteligente 24/7 a pantalla completa, Simulacros, **Flashcards con repetición espaciada** y Modo Enfoque — con **Tutor IA en Gemini + RAG persistido en disco** (rate-limited a 60 peticiones/hora), **Tablón de Convocatorias scrapeado de BOE/BOJA** (con un bug real de fechas encontrado y corregido, más un blindaje general contra excepciones), y un **banco de 700 preguntas de Simulacro + 210 flashcards generadas con RAG acotado por 7 temas** (Legislacion/General/Rescate/Sanitario/Incendio/Equipos de Intervencion/**Provincia**) — todo protegido de verdad por 403 en el backend, no solo por UI.

## Índice

| Archivo | Contenido |
|---|---|
| [01-stack-y-arquitectura.md](docs/handover/01-stack-y-arquitectura.md) | Stack (Gemini, no OpenAI), estructura de carpetas, **infraestructura real del VPS y flujo de despliegue** |
| [02-autenticacion-y-pagos.md](docs/handover/02-autenticacion-y-pagos.md) | Auth multitenant (rate-limited), `is_pro` vía webhook real de Stripe, recuperación de contraseña por Gmail SMTP real, soporte con trato Premium/Gratuito, **🔴 `ENABLE_STRIPE=false` activo — checkout pausado, lista de espera en su lugar** |
| [03-rendimiento-fisico-teorico-gamificacion.md](docs/handover/03-rendimiento-fisico-teorico-gamificacion.md) | MarcaFisica/SimulacroTeorico, `Workout` inactivo, heatmap real, Pomodoro, **Acondicionamiento Físico/Alto Rendimiento Teórico ahora gratis en el Dashboard (ya no Premium)** |
| [04-tutor-ia-y-rag.md](docs/handover/04-tutor-ia-y-rag.md) | RAG compartido — migrado a Gemini, **loader ahora también soporta `.docx`/`.xlsx`** (19.618 → **19.881 fragmentos en local**, VPS pendiente de rebuild), metadata `archivo` para filtrado por tema, rate limiting, restricción de IP de `GOOGLE_API_KEY` (resuelta), bugs de RAG corregidos (cabeceras repetidas, contexto de parque perdido, **citas cruzadas mal atribuidas — reforzado de nuevo**), y **19 normas oficiales añadidas tras calibrar contra un examen real de 59 preguntas y un documento de referencia del programa oficial** |
| [05-tablon-convocatorias-scraper.md](docs/handover/05-tablon-convocatorias-scraper.md) | Scraper BOE/BOJA — **bug real de fechas encontrado/corregido + blindaje general (excepciones, validación de tipos, red de seguridad)** |
| [06-simulacros-ia.md](docs/handover/06-simulacros-ia.md) | Banco precargado (no generación en vivo), RAG real acotado por tema, **7 temas (Provincia nueva)**, 700 preguntas generadas, **copy corregido (ya no "generado por IA")** |
| [07-deuda-tecnica-y-pendientes.md](docs/handover/07-deuda-tecnica-y-pendientes.md) | **Leer antes de tocar producción** — bloqueantes ordenados por impacto |
| [08-convenciones-de-codigo.md](docs/handover/08-convenciones-de-codigo.md) | Reglas de estilo/estructura + convenciones nuevas de despliegue/Gemini/cuotas |
| [09-zona-premium-y-upsell.md](docs/handover/09-zona-premium-y-upsell.md) | Zona Premium en **5 pestañas** (Tablón/Tutor/Simulacros/**Flashcards**/Modo Enfoque), 3 componentes de upsell con copy unificado, Acondicionamiento Físico salió de Premium |
| [10-flashcards-y-provincia.md](docs/handover/10-flashcards-y-provincia.md) | **Nuevo.** Módulo de Flashcards (repetición espaciada, SM-2 simplificado), categoría "Provincia" (Huelva) compartida con Simulacros, auditoría completa de `TEMA_A_ARCHIVOS` y limpieza de 11 documentos duplicados en `conocimiento/` |

## Los bloqueantes más urgentes ahora mismo

1. 🔴 **Credenciales reales sin `.gitignore` en la raíz del repo** (`backend/Internal Database URL.txt`, `client_secret_...json`) — siguen ahí y siguen trackeadas en git, sin resolver. El más crítico de todos, sigue sin tocarse.
2. 🔴 **`ENABLE_STRIPE=false` activo en producción** — no es un bug, es una pausa deliberada del checkout real (ver punto 6 de arriba), pero hay que saberlo antes de tocar nada de pagos.
3. 🟠 Login con Google sigue sin probarse en navegador real de extremo a extremo.
4. 🟠 Portal de Cliente de Stripe (`POST /api/pagos/portal`) sin probar de extremo a extremo (aunque ya se generó un `stripe_customer_id` real durante la prueba de checkout).
5. 🟠 **Dos commits sin desplegar al VPS** (2026-08-19): el selector de sexo con baremo real de pruebas físicas (`eafaf66`) y los Anexos 2/3 del PTEAnd para Geografía de Andalucía (`5dd7547`). Producción sigue con el motor de cálculo antiguo (sin campo `sexo`) y sin esos documentos indexados. Ver puntos 34 y 35 de [07](docs/handover/07-deuda-tecnica-y-pendientes.md). **El módulo de Flashcards + Provincia (`d4924cf`) ya SÍ está desplegado y verificado** (2026-08-22), ver punto 37.

Ya **no** son bloqueantes (resueltos, con verificación real, no solo "debería funcionar"): `DOMINIO_APP` hardcodeado, despliegue nunca hecho, checkout de Stripe nunca completado con tarjeta real, el índice de Chroma que se quedó a medias el 2026-08-08 tras chocar con la cuota de Gemini (reconstruido y verificado en local y VPS el 2026-08-09), `WEBHOOK_RECUPERACION_URL` sin enviar emails reales (ahora Gmail SMTP real), ausencia de rate limiting en auth/tutor, falta de cabeceras de seguridad/CSP, la restricción de IP de `GOOGLE_API_KEY` que tumbó Gemini por completo en el VPS (2026-08-11), y dos bugs reales de recuperación (RAG) del Tutor IA — cabeceras de página repetidas contaminando la búsqueda semántica y contexto de parque perdido entre páginas en TEMA-36, que llegaron a hacer que el tutor inventara una respuesta falsa (2026-08-11). Detalle completo, incluidos los pendientes menores, en [07](docs/handover/07-deuda-tecnica-y-pendientes.md).

## Qué está verificado end-to-end (no solo "debería funcionar")

- Auth completo (registro/login/Google estructural/recuperación), aislamiento multitenant.
- `is_pro` activado por un **checkout real de Stripe completado hasta el final** (modo test) contra producción, no solo un webhook firmado a mano — incluye el hallazgo de que Stripe presenta un hCaptcha invisible al detectar automatización en el pago.
- Dashboard físico/teórico + heatmap de actividad + 3 componentes de upsell hacia Premium, verificados en desktop/tablet/móvil.
- Zona Premium rediseñada en 3 pestañas (Dashboard/Inicio, Tutor IA a pantalla completa, Simulacros), sin scroll de página en ningún breakpoint, tour de onboarding adaptado y verificado paso a paso.
- Tutor IA (chat, plan de estudio por convocatoria) migrado por completo a Gemini, RAG sobre 30+ documentos reales de temario, persistido en disco.
- Banco de Simulacros (600 preguntas, 6 temas) generado con RAG real acotado por tema — verificado que cada tema recupera del documento correcto y no de otros por pura similitud semántica.
- Scraper de BOE/BOJA: un bug real de fechas (convocatoria de Huelva mostrada como vigente llevando cerrada desde junio) encontrado, corregido, y blindado con una red de seguridad + manejo de excepciones en todo el pipeline — verificado con una ejecución real contra los feeds en vivo, que descartó automáticamente otra convocatoria vencida sin intervención manual.
- **Seguridad backend (2026-08-10)**: CORS explícito, `SECRET_KEY` con fail-fast, límite de payload, cabeceras HTTP y una CSP calculada revisando lo que carga de verdad el frontend (no genérica) — verificado en local y en producción real sin ninguna violación de CSP en consola, navegando por todas las vistas.
- **Recuperación de contraseña por Gmail SMTP real** — sustituye al webhook de pruebas, verificado con un envío real confirmado por el usuario en su propia bandeja.
- **Soporte con trato Premium/Gratuito** en el formulario de contacto del Dashboard — verificado de extremo a extremo en producción con una cuenta Premium real (mensaje llega al admin marcado prioridad alta) y una gratuita (autorespuesta automática, nunca llega al admin).
- **`ENABLE_STRIPE` + lista de espera**: interruptor que pausa el checkout real sin tocar el resto del código, activo en producción ahora mismo — verificado que el botón real deja de crear sesiones de Stripe y abre el modal de espera, que el email se guarda sin duplicados, y que la autorespuesta se intenta enviar de verdad.
- **Fixes de móvil real**: menú hamburguesa del sidebar (antes "Zona Premium" y "Cerrar sesión" quedaban fuera de pantalla), y el zoom automático de iOS Safari al enfocar campos con letra <16px (afectaba a los 21 campos de la web) — ambos diagnosticados con datos reales de un iPhone, no adivinados.
- **Restricción de IP de `GOOGLE_API_KEY` en el VPS (2026-08-11)**: diagnosticada probando la misma clave desde AI Studio, desde el ordenador del usuario y desde el VPS por tres vías distintas (SDK, `requests`, `curl` nativo) hasta aislar que solo fallaba desde el servidor — resuelta añadiendo la IP del VPS a la lista permitida en Google Cloud Console, verificada con una llamada REST directa antes de dar el problema por cerrado.
- **2 bugs reales de recuperación (RAG) del Tutor IA (2026-08-11)**: cabeceras/pies de página repetidos que contaminaban la búsqueda semántica, y el contexto de a qué parque pertenece cada tabla de TEMA-36 que se perdía entre páginas (llegó a hacer que el tutor inventara que el documento "solo tenía datos de 3 parques" cuando en realidad cubre los 9). Ambos corregidos, auditados contra todo el corpus (no solo el documento que los motivó), y verificados con la pregunta real que originó el reporte más una batería de 16+ preguntas variadas sin regresiones. Ver [04](docs/handover/04-tutor-ia-y-rag.md).
- **Tutor IA calibrado contra un examen real completo (BOE 127/2026, 59 preguntas, 2026-08-15)**, comparado letra por letra contra la plantilla oficial de corrección — la mayoría de fallos eran huecos de contenido honestamente declinados (no inventados), no errores de razonamiento. Tras añadir 19 normas oficiales en 3 rondas (~14.965 → 19.618 fragmentos), la mayoría de esos huecos pasaron a responderse correctamente con cita literal. También se corrigió un bug real de citación cruzada (el tutor citó con seguridad una norma equivocada por formulación parecida a la correcta) y se auditó con 6 baterías más de preguntas variadas (150+ en total) sin encontrar fabricación de datos, fuga del rol, ni regresiones. Ver [04](docs/handover/04-tutor-ia-y-rag.md).
- **El mismo examen (las 59 preguntas completas esta vez, no una muestra) vuelto a pasar el 2026-08-17** tras el fix de tablas PDF y las 3 reglas de oro de esta sesión: **40/59 correctas (67,8%)**, 9 declinadas honestamente, 10 falladas — pero cero alucinaciones de datos inventados, incluso las falladas están ancladas a una cita real. Confirmó en la práctica el bloqueo permanente de la norma UNE 60250 (punto 28 de [07](docs/handover/07-deuda-tecnica-y-pendientes.md)) y encontró un patrón nuevo no corregido: en 3 preguntas el tutor respondió "Ninguna es correcta" con seguridad en vez de declinar cuando no encontró el dato exacto. Ver [04](docs/handover/04-tutor-ia-y-rag.md) para el desglose completo.
- **Loader del Tutor IA ampliado a `.docx`/`.xlsx` (2026-08-16)**, cerrando dos huecos reales que llevaban documentados desde el 2026-08-11: varios `.docx` (contenido de la provincia de Huelva, base de conocimiento de parques con 20 tablas) y `GENTILICIOS.xlsx` que el usuario había depositado en `conocimiento/` sin ningún efecto. Índice reconstruido y verificado en **local y en el VPS** (19.618 → 19.881 fragmentos en ambos) con preguntas reales sin regresión: los gentilicios responden bien desde el xlsx nuevo, y las preguntas que ya funcionaban (BC-2, TREBEP) siguen igual. De paso se reforzó el prompt contra el caso residual de cita cruzada que quedaba documentado (golpe de ariete/Decreto 36/2025) — verificado que ahora cita las fuentes de hidráulica correctas. Desplegado a producción: dependencias instaladas en el venv real del VPS, servicio reiniciado, `opotracker.tech` verificado respondiendo tras el reinicio.
- **Extracción real de tablas PDF con `pdfplumber`, mismo día**, motivada por un PDF real de tablas de pérdida de carga de mangueras que el usuario aportó para auditar al tutor con una herramienta nueva (`backend/auditar_tutor.py`, banco de preguntas + juez Gemini). Bug real confirmado con preguntas de valor conocido: `pypdf` aplanaba tablas de muchas columnas a listas de números sin estructura, y el tutor leía la columna equivocada con total seguridad (ej. "10 bares" cuando la tabla real decía "0,37 bar"). Corregido extrayendo la rejilla real de cada tabla con `pdfplumber` y reindexándola en Markdown — aplicado a los 67 PDF del corpus, no solo al documento nuevo. Efecto secundario real encontrado y corregido en la misma sesión: el crecimiento del índice (19.898 → 21.621 fragmentos) diluyó la recuperación de TEMA-36/BC-2 por debajo del top-10, resuelto subiendo `FRAGMENTOS_A_RECUPERAR` de 10 a 15. De la misma auditoría salieron 3 "reglas de oro" nuevas en el `SYSTEM_PROMPT` (fuente única, anti-complacencia, cita obligatoria y pertinente). Verificado en local con 5 preguntas de valor conocido (antes 2/5 mal, ahora 5/5 bien) más la batería de regresión completa sin fallos.
- **Desplegado al VPS (2026-08-17)** con una incidencia real de infraestructura en el camino: el rebuild del índice con `pdfplumber` mataba el proceso por falta de memoria (VPS con solo 3,8GB de RAM, sin swap — confirmado con `dmesg`, `Out of memory: Killed process`, dos veces seguidas). Resuelto con un swapfile de 4GB que el usuario creó desde su propia terminal (la creación de swap por SSH la bloqueó correctamente el clasificador de seguridad de Claude Code). El usuario decidió explícitamente dejar el swap activo de forma permanente, no es un pendiente de limpieza — ver [01-stack-y-arquitectura.md](docs/handover/01-stack-y-arquitectura.md). Rebuild del VPS verificado: 21.621 fragmentos (igual que en local), preguntas de valor conocido correctas contra el índice real, servicio reiniciado, `opotracker.tech` respondiendo 200.
- **Auditoría sistemática de los 50 temas del Anexo 1 del BOP Huelva 80/2026 contra la base de conocimiento (2026-08-19)**, comparando el temario oficial completo (extraído del propio PDF, no un resumen) tema por tema. Encontró que `Anuncio_356176_BOP-80_2026.pdf` y `BOJA25-032-00076.pdf` contienen el listado del Anexo 1 como simple índice de títulos, lo que daba falsos positivos de "cobertura" en una primera búsqueda por palabra clave — hubo que excluirlos y verificar cada candidato con una pregunta real al tutor antes de confirmar un hueco. Bloque 3 (temas propios del CPBH) al 100%. Encontró y cerró un hueco real: Geografía de Andalucía (Tema 1, Bloque 2) — el tutor respondía mezclando datos de la provincia de Huelva con la pregunta sobre Andalucía completa y no cubría vegetación en absoluto — cerrado con los **Anexos 2 y 3 del PTEAnd** (Decreto 69/2024, BOJA 2024/47), la bibliografía oficial exacta que cita el propio tema. Descartó dos falsos huecos (Derecho Administrativo y Hacienda Pública) que ya estaban bien cubiertos por normas añadidas en sesiones anteriores. Índice local reconstruido (21.621 → 21.832 fragmentos). **Sin desplegar al VPS** (commit `5dd7547`, solo pedido commitear).
- **Selector de sexo con baremo oficial real de pruebas físicas (2026-08-19)**, cerrando el hueco entre `baremos_fisicas.json` (ya en el repo desde antes) y el motor de cálculo, que usaba interpolación continua sin distinción de sexo. Verificado en el navegador real: mismas marcas dan puntuaciones distintas por sexo, recalculo en vivo sin recargar, guardado persistiendo correctamente. **Sin desplegar al VPS** (commit `eafaf66`, solo pedido commitear) — producción sigue con el motor de cálculo antiguo.

Ver cada archivo de `docs/handover/` para el detalle de cómo se verificó cada pieza.
